#include <stdio.h>
int main(){
    int n;
    int arr[n];
    scanf("%d", &n);
    for(int i=0; i<=n; i++){
        scanf("%d", &arr[i]);
    }
    for(i=0; i<n; i++){
        if(arr[i] < arr[i+1]{
            printf("%s", Sorted );
        })
        else{
            printf("%s", Not Sorted);
        }
    }
    return 0;
    
}